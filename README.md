# Linux bash Log Analyzer
its a script that analyzes logs in Linux systems making it easier to read logs,
script itself shows if it finds the keyword inside of log files and if it finds, it shows which line its in alongside the code. 

